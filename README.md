# ZenTurf

Chennai venue booking app.

## Setup

1. `bun install` (or `npm install`)
2. Copy `.env.example` to `.env` and fill in Supabase credentials
3. `bun run dev`

## Stack

React 19, TanStack Start, Vite 7, Tailwind v4, shadcn/ui, Supabase, Leaflet.
